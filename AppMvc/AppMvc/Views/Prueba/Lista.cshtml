
@model List<AppMvc.Models.Album>
@{
    ViewBag.Title = "Lista";
    //List<AppMvc.Models.Album> albunes = ViewBag.Albums;
}


<h1>Lista</h1>
<h2>Lista de Albunes:</h2>
    @foreach (var a in (Model))
    {
        <li>@a.Title</li>
    }
    
@{
    // this is a block of code. For demonstration purposes,
    // we'll create a "model" inline.
    //var items = new string[] { "one", "two", "three", "Cuatroooooo" };
}
<html>
<head>
    <title>Otro ejemplo..</title>
</head>
<body>
    <h1>Listando @Model.Count items.</h1>
    <ul>
        @foreach (var a in Model)
        {
            <li>The item is @a.Item</li>
        }
    </ul>
</body>
</html>

<p>
    You should follow
    @@aspnet
</p>

<h2>Como vemos de esta forma lo que me muestra esta mal..</h2>
@{
    string mensaje = "<script>alert('haacked!');</script>";
}
<span>@mensaje</span>

<h2>Esta si seria una buena manera, agregando "@@Html.Raw" adelante de mensaje </h2>

@{
    string message = "<strong>This is bold!</strong>";
}
<span>@Html.Raw(message)</span>

